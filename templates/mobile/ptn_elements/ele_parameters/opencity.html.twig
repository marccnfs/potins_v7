<div class="_fx-R fx-c">
    <div class="pwa-space-tag-when">
        <div class="space-tag">
            <div class="bull_tag active" >
                {% if openday is defined  %}
                    {% if openday.state %}
                        <div class="-br-Ma"><i class="fas fa-door-open"></i>&nbsp;ouvert</div>
                    {% else %}
                        <div class="-br-Ma"><i class="fas fa-door-closed"></i>&nbsp;fermé</div>
                    {% endif %}
                {% else %}
                    <div></div>
                {% endif %}
            </div>
        </div>
    </div>
</div>

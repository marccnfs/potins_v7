{% set hasUpcomingEvent = notice.hasUpcomingEvent ?? false %}

<a class="affiche"
   href="{{ path('show_potin', {'slug': notice.post.slug, 'id': notice.post.id}) }}"
   aria-label="{{ notice.post.titre }}">
    {% if hasUpcomingEvent %}
        <span class="potin-event-badge potin-event-badge--mobile">
            <span class="potin-event-badge__icon" aria-hidden="true">📅</span>
            <span class="potin-event-badge__text">Évènement à venir</span>
        </span>
    {% endif %}
    <div class="evnet_titre_v8">{{ notice.post.titre }}</div>
    {% if notice.post.subject is not same as (" ") and notice.post.subject is not empty %}
        <div class="_fx-R_v2 _fx-convers_inv_v5">
            <div class="info-post_v5">
                <div class="event_second_titl_v8">{{ notice.post.subject|striptags|u.truncate(120, '…') }}</div>
            </div>
        </div>
    {% endif %}
    {% if notice.post.media.imagejpg[0].namefile is defined %}
        <img class="img-allnotice"
             src="{{ asset('upload/module/' ~ notice.post.media.imagejpg[0].namefile) }}"
             alt="{{ notice.post.titre }}"
             loading="lazy">
    {% endif %}

</a>

<div class="boarder">
    {% if notice.board is defined %}
    <div class="flx-lik-_v5">
        {% if notice.board.template is defined and notice.board.template.logo %}
            <div class="wb-logo-lite">
                <div class="bkgrndlogo"
                     style="background-image: url('{{ asset('spaceweb/template/' ~ notice.board.template.logo.namefile) }}')"
                     role="presentation"></div>
            </div>
        {% endif %}
    </div>

  {% endif %}
</div>

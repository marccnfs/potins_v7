<div class="cub-form">
    <div class="cbe_cnt">
        <div class="c_i_b">
            <div class="in_cib">

                <div class="form-groupfields glass-label">
                    <div class="glass-title_form">
                        <h3>Sélectionez une date</h3>
                    </div>

                    <div class="process-module_glass">
                        <div class="stape-process-lit" >
                            <h5>atelier : <strong>{{ post.titre }}</strong></h5>
                        </div>
                        {{ form_start(form) }}
                        {{ form_errors(form) }}

                        <select name="{{ field_name(form.daychoice) }}" required class="form-control">
                            <option value="">{{ field_label(form.daychoice) }}</option>

                            {% for label, value in field_choices(form.daychoice) %}
                                <option value="{{ value }}">{{ label }}</option>
                            {% endfor %}
                        </select>

                        {{ form_rest(form) }}
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

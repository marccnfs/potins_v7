{% block body %}
    <div class="_wb">
        <div class="container-bo_v5">
            {{ include('aff_messagery/menu/messagemenu_customer.html.twig') }}
            <section class="_wb-ospace">
                <div class="head-msg-space">
                    <div class="msg-pag">
                        <div class="zone-inf-msg">
                            <div class="nav-msg">
                               {#  <div class="pop_op-msg" >Toutes vos conversations ...</div> #}
                                {% if pagination is defined %}
                                    <div id="mormsg" id="af_navdk">
                                        {{ knp_pagination_render(pagination) }}
                                    </div>
                                {%  endif %}
                            </div>
                        </div>
                    </div>
                </div>
                <div class="msg_cub" id="typeform" data-type="ctc">
                    <div class="contener-msg">
                        {{ include('aff_messagery/customer/view/list_allmessage.html.twig') }}
                    </div>
                </div>
            </section>
        </div>
     {#    {{ include('footer/'~agent~'/affichange_footer_rel.html.twig') }}  #}
    </div>
{%  endblock body %}
{% use('js/js_customer.html.twig') %}
{% block javascripts %}
    {{ parent() }}
{% endblock %}
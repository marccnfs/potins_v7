{% block body %}
<div class="_o_media">
    <div class="act-mem">
        {% for message in app.flashes('notice') %}
            <div class="_fx-Tflhs">
                <div class="comd-flhs">
                    <h5>attention !</h5>
                    <div class="_rlm-a">
                        <div class="msg-flhs">{{ message }}.</div>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>
    {{ include('desk/ptn_elements/ele_events/modale_resa_media_event.html.twig') }}
</div>
    {{ include('footer/'~agent~'/affichange_footer_rel.html.twig') }}
{%  endblock body %}
{% use "js/js_customer.html.twig" %}
{% block javascripts %}
    {{ parent() }}
{% endblock %}

<div class="navadmin">
    <div class="navaffi-bk">
        <div class="afnav-fl">
            <a href="{{ path('potins_index') }}" class="navlogo_affi">
                <div class="navlogo-svg">{{ include('svg/potins/logopotins_svg_small.html.twig') }}</div>
            </a>
        </div>

        <div class="afnav-fl">
            <div class="head-bull_admin">
                    <h2>{{ board.nameboard }}</h2>
            </div>
        </div>

        <div class="tools-menu">
            {#    <a class="e-tool" title="parmaetre du panneau" href="{{ path('members',{'id':board.id}) }}"><i class="fa fa-users" aria-hidden="true"></i></a> #}
        {#  pour l'insatnt pas de gestion des commentaires :    <a class="e-tool" title="messagerie"  href="{{path('board_board',{'id': board.id }) }}"><i class="fa fa-comment-o" aria-hidden="true"></i></a> #}
            {#   <a class="e-tool" title="messagerie" href="{{ path('search_partner',{'id':board.id}) }}"><i class="fa fa-cloud" aria-hidden="true"></i></a> #}
            <a class="e-tool" title="paramètres" href="{{ path('parameters',{'id':board.id}) }}"><i class="fa fa-cog" aria-hidden="true"></i></a>
        </div>
    </div>
    <div class="navaffi-bk">
        {{ include('desk/pop/pop_menu_accueil.html.twig') }}
        {#
        {{ include('desk/pop/modal-pop-office.html.twig') }}
      <div class="afnav-fl">
          <div class="afnav-link-brd">
              <a href="{{ path('show_board',{'slugboard':board.slug}) }}" class="afnav-alk">voir le panneau public</a>
          </div>
      </div>
        #}
    </div>
</div>
<div class="pg_other">
    <div class="board-list-replace">
{#
        <nav class="other">
            <div class="nogo"><h3>et aussi</h3></div>
        </nav>
#}
        <div class="flex-notb_v6-fix">
            {% for otherpost in potins|slice(0, 6) %}
                <div class="last-postall_v6-fix">
                    {{include('desk/ptn_elements/ele_boards/poppost.html.twig')}}
                </div>
            {% else %}
                <div class="affiche">
                    <div class="evnet_titre_v5">pas d'autre potin actuellement !!</div>
                </div>
            {%  endfor %}
        </div>

    </div>
</div>
<div class="nav_game">
    {# >>> Passe les variables dont le sous-menu a besoin #}
    {{ include('desk/ptn_menus/menu_header.html.twig', {
        participant: participant|default(null),
        brand_route: brand_route|default('home'),
        active: active|default(''),
        nav: nav|default({})
    }) }}
</div>

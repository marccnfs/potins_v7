<div class="menu-bo_sp">
    <div class="menu-container">
        <div class="menu-item">
            {% if participant is not null %}
            <section class="progression">
                <h2>🧩 Ta progression</h2>
                <ul>
                    {% for i in 1..6 %}
                        <li>
                            {% set unlocked = i == 1 or (i - 1) in progression %}
                            {% if unlocked %}
                                <a href="{{ path('escape_start', {'step': i}) }}">
                                    {% if i in progression %}✅{% else %}🟡{% endif %} Étape {{ i }}
                                </a>
                            {% else %}
                                🔒 Étape {{ i }}
                            {% endif %}
                        </li>
                    {% endfor %}
                </ul>
            </section>
            {% endif %}
        </div>
    </div>
</div>

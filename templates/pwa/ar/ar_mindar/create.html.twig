<div class="container"
     data-controller="mindar-create"
     data-mindar-create-scenes-endpoint-value="{{ path('api_ar_scene_create') }}"
     data-mindar-create-upload-endpoint-value="{{ path('api_upload_mind') }}"
     data-mindar-create-cancel-url-value="{{ path('ar_gallery') }}">
    <h1>Créateur de scène RA (MindAR)</h1>

    <form id="form-create" data-mindar-create-target="form">
        <div class="form-group">
            <label for="title">Titre de la scène</label>
            <input type="text"
                   id="title"
                   name="title"
                   data-mindar-create-target="title"
                   placeholder="Ma scène RA"
                   value="">
        </div>

        <!-- Sélecteur de pack -->
        <div class="form-group mb-4">
            <label for="pack" class="block text-sm font-medium text-gray-700 mb-1">
                Pack d'images cibles
            </label>
            <p class="text-xs text-gray-500 mb-2">Choisissez le pack contenant les images de suivi MindAR.</p>

            <select id="pack"
                    name="pack"
                    class="form-select w-full rounded border border-gray-300 p-2"
                    data-mindar-create-target="pack"
                    data-action="change->mindar-create#packChanged">

                {% if packs|length > 0 %}
                <option value="" selected disabled>Choisissez un pack MindAR…</option>
                    {% for pack in packs %}
                        <option value="{{ pack.mindPath }}"
                                data-packname="{{ pack.name }}"
                            {% if pack.jsonPath is defined and pack.jsonPath %}
                                data-json="{{ pack.jsonPath }}"
                            {% endif %}
                            {% if pack.thumbnail is defined and pack.thumbnail %}
                                data-thumbnail="{{ pack.thumbnail }}"
                            {% endif %}
                            {% set items = pack.metadata.items ?? [] %}
                            {% if items is not empty %}
                            data-items='{{ items|json_encode|e('html_attr') }}'
                            {% endif %}>
                            {{ pack.name }}
                        </option>
                    {% endfor %}
                {% else %}
                    <option value="" selected disabled>Aucun pack disponible </option>
                {% endif %}
            </select>

            <div id="pack-preview"
                 class="pack-preview-wrapper mt-3"
                 data-mindar-create-target="packPreview">
                <p class="text-sm text-gray-500">Sélectionnez un motif pour afficher un aperçu.</p>
            </div>
        </div>

        <div class="form-group">
            <label class="block text-sm font-medium text-gray-700 mb-1" for="targetIndex">
                Motif de reconnaissance
            </label>
            <p class="text-xs text-gray-500 mb-2">Cliquez sur l'une des images du pack pour définir la cible MindAR utilisée pour l'ancrage 3D.</p>

            <div class="target-grid" data-mindar-create-target="thumbs">
                <p class="text-sm text-gray-500">Aucun pack MindAR détecté pour le moment.</p>
            </div>
            <p class="text-xs text-blue-700 mt-2" data-mindar-create-target="targetInfo">
                Sélectionnez une image pour confirmer la détection MindAR.
            </p>
            <input type="hidden" id="targetIndex" name="targetIndex" value="" data-mindar-create-target="targetIndex">
        </div>

        {% set hasModels = models is defined and models|length > 0 %}

        <fieldset class="form-group">
            <legend class="block text-sm font-medium text-gray-700 mb-2">
                Type de contenu
            </legend>
            <p class="text-xs text-gray-500 mb-3">Sélectionnez le type d'élément virtuel à afficher sur la cible.</p>
            <div class="flex flex-wrap gap-4" role="radiogroup">
                <label class="inline-flex items-center gap-2 text-sm">
                    <input type="radio"
                           name="assetType"
                           value="model"
                           class="radio"
                           data-mindar-create-target="assetType"
                           data-action="mindar-create#assetTypeChanged"
                           checked>
                    <span>Modèle 3D (.glb)</span>
                </label>
                <label class="inline-flex items-center gap-2 text-sm">
                    <input type="radio"
                           name="assetType"
                           value="video"
                           class="radio"
                           data-mindar-create-target="assetType"
                           data-action="mindar-create#assetTypeChanged">
                    <span>Vidéo (mp4/webm)</span>
                </label>
                <label class="inline-flex items-center gap-2 text-sm">
                    <input type="radio"
                           name="assetType"
                           value="image"
                           class="radio"
                           data-mindar-create-target="assetType"
                           data-action="mindar-create#assetTypeChanged">
                    <span>Image 2D (png/jpg)</span>
                </label>
            </div>
        </fieldset>

        <input type="hidden" id="asset" name="asset" data-mindar-create-target="asset" value="">

        <div class="form-group" data-mindar-create-target="modelSection">
            <label class="block text-sm font-medium text-gray-700 mb-1" for="asset">
                Bibliothèque de modèles 3D
            </label>
            <p class="text-xs text-gray-500 mb-3">Choisissez l'animation 3D affichée lorsque l'image déclencheur est détectée.</p>

            {% if hasModels %}
                <div class="model-grid" data-mindar-create-target="modelChoices">
                    {% for model in models %}
                        <button type="button"
                                class="model-card"
                                data-mindar-create-target="modelChoice"
                                data-action="mindar-create#selectModel"
                                data-model-path="{{ model.path }}"
                                data-model-name="{{ model.name }}"
                                data-model-description="{{ model.description|default('') }}"
                                data-model-emoji="{{ model.emoji|default('') }}"
                                {% if loop.first %}data-model-default="1"{% endif %}>
                            <span class="model-card__icon" aria-hidden="true">{{ model.emoji|default('🧊') }}</span>
                            <span class="model-card__title">{{ model.name }}</span>
                            {% if model.description is defined and model.description %}
                                <span class="model-card__description">{{ model.description }}</span>
                            {% endif %}
                        </button>
                    {% endfor %}
                </div>
                <div class="model-info" data-mindar-create-target="modelInfo">
                    <p class="text-xs text-gray-500">Sélectionnez un modèle pour afficher ses détails.</p>
                </div>
            {% else %}
                <div class="alert alert-warning">
                    Aucun modèle 3D n'est disponible pour le moment.
                </div>
            {% endif %}
        </div>

        <div class="form-group hidden" data-mindar-create-target="videoSection">
            <label class="block text-sm font-medium text-gray-700 mb-1" for="videoUrl">
                URL de la vidéo
            </label>
            <p class="text-xs text-gray-500 mb-2">Fichier mp4/webm compatible streaming. Activez la lecture automatique en préparant la vidéo sans son ou en mode silencieux.</p>
            <input type="url"
                   id="videoUrl"
                   class="form-input"
                   placeholder="https://cdn.example.com/videos/ar.mp4"
                   data-mindar-create-target="videoInput"
                   data-action="input->mindar-create#updateAssetFromInput change->mindar-create#updateAssetFromInput">
        </div>

        <div class="form-group hidden" data-mindar-create-target="imageSection">
            <label class="block text-sm font-medium text-gray-700 mb-1" for="imageUrl">
                URL de l'image
            </label>
            <p class="text-xs text-gray-500 mb-2">PNG/JPG avec transparence optionnelle. L'image est affichée sur un plan 2D.</p>
            <input type="url"
                   id="imageUrl"
                   class="form-input"
                   placeholder="https://cdn.example.com/images/overlay.png"
                   data-mindar-create-target="imageInput"
                   data-action="input->mindar-create#updateAssetFromInput change->mindar-create#updateAssetFromInput">
        </div>

        <div class="form-group">
            <label for="sound">Ambiance sonore</label>
            <select id="sound" name="sound" data-mindar-create-target="sound">
                <option value="">Aucune</option>
                <option value="/build/audio/forest.mp3">Forêt</option>
                <option value="/build/audio/water.mp3">Eau</option>
            </select>
        </div>

        <fieldset class="form-group">
            <legend class="block text-sm font-medium text-gray-700 mb-2">Placement de l'élément</legend>
            <p class="text-xs text-gray-500 mb-3">Utilisez les valeurs numériques pour positionner précisément le contenu virtuel sur la cible MindAR.</p>

            <div class="grid placement-grid">
                <div>
                    <h3 class="placement-grid__title">Position</h3>
                    <div class="placement-grid__row">
                        <label for="positionX">X</label>
                        <input type="number" step="0.01" id="positionX" value="0" data-mindar-create-target="positionX" data-action="input->mindar-create#transformChanged">
                    </div>
                    <div class="placement-grid__row">
                        <label for="positionY">Y</label>
                        <input type="number" step="0.01" id="positionY" value="0" data-mindar-create-target="positionY" data-action="input->mindar-create#transformChanged">
                    </div>
                    <div class="placement-grid__row">
                        <label for="positionZ">Z</label>
                        <input type="number" step="0.01" id="positionZ" value="0" data-mindar-create-target="positionZ" data-action="input->mindar-create#transformChanged">
                    </div>
                </div>
                <div>
                    <h3 class="placement-grid__title">Rotation (°)</h3>
                    <div class="placement-grid__row">
                        <label for="rotationX">X</label>
                        <input type="number" step="1" id="rotationX" value="0" data-mindar-create-target="rotationX" data-action="input->mindar-create#transformChanged">
                    </div>
                    <div class="placement-grid__row">
                        <label for="rotationY">Y</label>
                        <input type="number" step="1" id="rotationY" value="0" data-mindar-create-target="rotationY" data-action="input->mindar-create#transformChanged">
                    </div>
                    <div class="placement-grid__row">
                        <label for="rotationZ">Z</label>
                        <input type="number" step="1" id="rotationZ" value="0" data-mindar-create-target="rotationZ" data-action="input->mindar-create#transformChanged">
                    </div>
                </div>
                <div>
                    <h3 class="placement-grid__title">Échelle</h3>
                    <div class="placement-grid__row">
                        <label for="scaleX">X</label>
                        <input type="number" step="0.01" id="scaleX" value="1" data-mindar-create-target="scaleX" data-action="input->mindar-create#transformChanged">
                    </div>
                    <div class="placement-grid__row">
                        <label for="scaleY">Y</label>
                        <input type="number" step="0.01" id="scaleY" value="1" data-mindar-create-target="scaleY" data-action="input->mindar-create#transformChanged">
                    </div>
                    <div class="placement-grid__row">
                        <label for="scaleZ">Z</label>
                        <input type="number" step="0.01" id="scaleZ" value="1" data-mindar-create-target="scaleZ" data-action="input->mindar-create#transformChanged">
                    </div>
                </div>
            </div>
        </fieldset>

        <div class="form-group">
            <label for="mindfile">Fichier cible (.mind)</label>
            <input type="file" id="mindfile" accept=".mind" data-mindar-create-target="mindfile" />
        </div>

        <div class="form-group flex flex-wrap gap-3 items-center justify-between">
            <div class="flex flex-wrap gap-2">
                <button type="button" class="btn" data-action="mindar-create#preview">Prévisualiser</button>
                <button type="button" class="btn btn-secondary" data-action="mindar-create#save">Enregistrer la scène</button>
            </div>
            <div class="flex flex-wrap gap-2 text-sm">
                <button type="button" class="btn btn-light" data-action="mindar-create#newScene">Nouvelle scène</button>
                <button type="button" class="btn btn-light" data-action="mindar-create#resetForm">Réinitialiser</button>
                <button type="button" class="btn btn-tertiary" data-action="mindar-create#cancel">Annuler</button>
            </div>
        </div>
    </form>

    <div class="divider"></div>

    <div id="preview" class="preview" data-mindar-create-target="preview" aria-live="polite">
        <div class="preview-placeholder">
            <p><em>Utilisez le bouton de prévisualisation pour ajuster le placement sans lancer la détection.</em></p>
        </div>
    </div>

    <div class="share-panel hidden" data-mindar-create-target="sharePanel">
        <h2 class="share-panel__title">Expérience publiée</h2>
        <p class="text-sm text-gray-600">Partagez ce lien ou le QR code pour lancer la scène RA sur mobile ou ordinateur.</p>
        <p class="mt-2">
            <a href="#" class="link" data-mindar-create-target="shareLink" target="_blank" rel="noopener">Voir la page de partage</a>
            ·
            <a href="#" class="link" data-mindar-create-target="experienceLink" target="_blank" rel="noopener">Ouvrir directement l'expérience</a>
        </p>
        <div class="share-panel__qr" data-mindar-create-target="shareQr"></div>
    </div>
</div>
